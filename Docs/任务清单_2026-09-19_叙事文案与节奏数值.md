# 任务清单 · 2026-09-19 · 叙事文案 + 局内节奏 + 数值平衡

> 来源：用户 2026-09-19 反馈（8 项）。
> 本文件只做**需求拆解 / 依赖分析 / 任务派工**，不含实现代码。
> 落地约束：改数值先改表再 Cook；不改 `.prefab`；C# 交 GLM5.3 Flash，Shader/渲染数学交 DeepSeek V4.1 Flash。

---

## 〇、现状核查结论（先读，避免做错）

| # | 核查项 | 结论 | 证据 |
|---|---|---|---|
| 1 | 玩家攻击频率是否读表 | **是，读表**。`AttackSpeed = 1 / 攻击间隔` | `Config/PlayerJobBaseStats.cs:70` 读 c[9]；`:126-127` 写 `AttrType.AttackSpeed` |
| 2 | 攻击间隔的隐藏钳制 | 有**攻击动画锁 0.5s**（`UnitAnimation.cs:38` `_attackAnimDuration = 0.5f`）。间隔 < 0.5 的表值不会更快 | 改表时新间隔必须 **≥ 0.55**，否则无效 |
| 3 | 玩家移速是否读表 | **是**。`ms = BASE_MOVE_SPEED(0.96f) × (表列 / 100)` | `Config/PlayerJobBaseStats.cs:122`；`GameConfig.cs:319` |
| 4 | 怪移速是否读表 | **否（关键坑）**：非 Boss 怪一律 `GameConfig.MONSTER_DEFAULT_MOVE_SPEED = 0.6912f`（`Monster.cs:726`），**不读 `monster_stats.csv`**；Boss 才读表且被 `Mathf.Min(表值, 0.6912×1.5=1.0368)` 钳住 | `Monster.cs:729-731`、`GameConfig.cs:352` |
| 5 | 表里 moveSpeed=2.2 的量纲 | 与世界单位不一致（Boss 实际被钳到 1.0368）。**不能**直接拿 2.2÷2 当"减半" | 需先统一量纲（见 T5） |
| 6 | 换道速度下限 | `UnitBase.cs:804` `laneSpeed = Mathf.Max(0.55f, MoveSpeed × 0.85f)`。玩家移速减半后 0.48×0.85=0.41 **< 0.55**，会被钳住 → 换道比直行还快，手感反常 | 必须一并处理（见 T5） |
| 7 | 章节战斗前文案例子 | `Story/ChapterStoryBeats.cs` `IntroTitle(ch)` / `OpeningLine(ch)`；仅 2 行、≤40 字、停留 2.5s | `ChapterSplashOverlay` 显示 |
| 8 | 章节遮罩是否全遮 | **是**：`ChapterSplashOverlay.cs:59` `bg.color = new Color(0,0,0,1f)` 纯黑不透明 | 需改半透 |
| 9 | Loading 文案来源 | `UI/LoadingTips.cs`（剧情句，带章节门槛防剧透）+ `UI/LoadingFlavorText.cs`（杂记），杂记混入率 `MercNoteChance = 0.35` | 用户要"更多世界观/艾丽娅" → 调低杂记占比并扩充剧情池 |
| 10 | Boss 后剧情钩子（现成对称位） | `ChapterStoryBeats.TryPlayPostBoss(ch, onDone)` | 新增 `TryPlayPreBattle` 与之对称 |
| 11 | 进关后剧情的插入窗口 | `GameConfig.FIRST_WAVE_SPAWN_DELAY = 1.5f`（首波前） | 作"进关后剧情"挂载点 |
| 12 | 波次结构入口 | `Combat/WavePlanner.cs`、`SetupBossWave(stageIdx)`（`:965`）；Boss 关**不走** `stage_mode` 表 | 见 stage_mode.csv 表头注释 |

---

## 一、任务清单

### T1 · Loading 文案：加世界观 + 玩家×艾丽娅回忆线

| 项 | 内容 |
|---|---|
| 负责 | GLM5.3 Flash（C# 数据类） |
| 改动文件 | `Assets/Scripts/UI/LoadingTips.cs`、`Assets/Scripts/UI/LoadingFlavorText.cs` |
| 架构方案 | ① `LoadingTips` 新增第三池 `MemoryTips`（玩家×艾丽娅回忆），并新增 `MemoryGate`（按通关章节解锁，防剧透）。② 三池按阶段配比抽取：前期 Town/Battle 为主、Memory 少量；第 3 章起 Memory 权重上升；第 6 章后以 Memory 为主。③ `MercNoteChance` 0.35 → 0.20（杂记只做调味，主线信息量优先）。④ `LoadingFlavorText` 追加「艾丽娅回忆录」分组（独立数组 + 独立洗牌，不与佣兵杂记混洗）。 |
| 文案素材（策划给，不要模型发明主线事实） | 三枚铜板买面包 / 转正仪式 / 她是介绍你进公会的人 / 第七支被挑中的小队 / 她出发前说的话。已有事实来源：`Docs/像素冒险：裂隙之刃_叙事重制策划案_V2.0.md`（艾丽娅 = `StoryPortraits.Xiaomei`） |
| 硬约束 | 每条必须带 gate；第 5–8 章的转折句**不得**在新手期出现 |
| 验收 | 新建档连点 20 次 Loading，无任何后段剧透句；第 7 章后再点，Memory 句占比明显上升 |

### T2 · 章节战斗前文案例子：地图信息 + 出怪缘由 + 剧情铺垫（两段式）

| 项 | 内容 |
|---|---|
| 负责 | GLM5.3 Flash |
| 改动文件 | `Assets/Scripts/Story/ChapterStoryBeats.cs`（主）、`Assets/Scripts/UI/ChapterSplashOverlay.cs`（承载）、`Assets/Scripts/Managers/BattleManager.cs`（挂载点） |
| 架构方案 | ① `OpeningLine(ch)` 从"两行氛围"升级为**结构化数据**：`ChapterIntro { 副标题 / 地图信息 / 出怪缘由 / 悬念句 }`，每章 4 段、总长 ≤ 90 字。② **两段式时序**：进关前 = `ChapterSplash` 只出「标题 + 副标题 + 地图信息」2 行（≤2.5s）；进入关卡后、首波刷怪前（`FIRST_WAVE_SPAWN_DELAY` 窗口内）= 新增 `TryPlayPreBattle(ch, onDone)` 出 2–4 句剧情对话，复用 `StoryDirector`。③ 与既有 `TryPlayPostBoss` 对称，共用 `StoryDirector.Ensure().Play`，不新增 UI 面板。 |
| 内容口径（8 章） | 每章必须回答三件事：**这是什么地方 → 为什么这里有怪 → 与主线（找艾丽娅 / 第七支小队 / 沉座）什么关系**。怪的成因绑定到章节设定：2 章亡灵外逃（怕更深的东西）、3 章植被疯长+有人留路标、5 章补给船编号、6 章沉座搏动、7 章"不是火的热"、8 章终局。 |
| 硬约束 | 可跳过（点击/倒计时）；不得阻塞首波刷怪超过 3s；不改任何 `.prefab` |
| 验收 | 第 1–8 章逐章过一遍：进关前 2 行 + 进关后 2–4 句；点击可跳过；不卡波次 |

### T3 · 章节文字出现效果（渐隐渐现 / 逐字）

| 项 | 内容 |
|---|---|
| 负责 | GLM5.3 Flash（UGUI 动画）；若要做溶解/描边流光等 Shader 效果 → DeepSeek V4.1 Flash |
| 改动文件 | `Assets/Scripts/UI/ChapterSplashOverlay.cs`（`RunRoutine`） |
| 架构方案 | ① 现状只有整体 `CanvasGroup.alpha` 淡出（`:144`），标题/正文是"硬出现"。② 改为三段：FadeIn(0.35s) → Hold → FadeOut；标题与正文**错峰**（正文延后 0.15s）。③ 正文可选**逐字显示**（打字机，速度 ≈ 25 字/秒，总时长不超过 Hold）。④ 全部走 `Time.unscaledDeltaTime`，不受暂停/慢动作影响（与现有实现一致）。 |
| 硬约束 | 点击跳过时立即 SetComplete（已显示全文本），不要半句消失 |
| 验收 | 渐入渐出平滑；逐字可被点击打断并补全；低端机不掉帧 |

### T4 · 遮罩改半透（不再全遮 UI）

| 项 | 内容 |
|---|---|
| 负责 | GLM5.3 Flash |
| 改动文件 | `Assets/Scripts/UI/ChapterSplashOverlay.cs:59` |
| 架构方案 | `bg.color` alpha 由 `1f` → 建议 **0.62**（半透，文字仍清晰）；`blocksRaycasts` 保持 true（保留点击跳过），但 `bg.raycastTarget` 需评估：半透后玩家可能希望点到底层 UI → 建议由调用方传 `blockInput` 参数，章节过场 `true`、Loading 类 `false`。 |
| 全局规则（务必写进备忘） | **今后任何遮罩一律不超过半透**：新增统一常量（如 `GameConfig.UiMaskMaxAlpha = 0.65f`），所有全屏遮罩走它，禁止再写 `alpha=1` 的纯黑/纯色全屏遮罩。 |
| 验收 | 过场时能看到底层 HUD 轮廓但读得清文字；不再出现"整屏纯黑" |

### T5 · 移动速度减半（改表 + 补齐表驱动通道）

| 项 | 内容 |
|---|---|
| 负责 | GLM5.3 Flash |
| 现状 | 玩家读表 ✅ / 非 Boss 怪**不读表** ❌ / Boss 怪读表但被钳 ❌ |
| 子任务 | **T5.1** 玩家：`Assets/Data/Source/Tables/player_job_base_stats.csv` 「基础移速」列整体 ×0.5（100→50、90→45、110→55、115→57.5、95→47.5），改完跑 `Tools/Data/Cook Tables`，源 CSV 与 `Resources/Data/Tables/*.bytes` 逐字一致。<br>**T5.2** 非 Boss 怪：把 `Monster.cs:726` 的常量改为**读表**（新增/复用 `monster_stats.csv` 的 `moveSpeed` 列 + 量纲换算），再在表里把 2.2 → 1.1；钳制上限 `MONSTER_DEFAULT_MOVE_SPEED × 1.5` 同步下调，否则改表被钳无效。<br>**T5.3** 量纲统一：先敲定「表值 → 世界单位」的换算系数（当前 2.2 被钳到 1.0368，说明表值非世界单位），**先定系数再改数值**。<br>**T5.4** 修 `UnitBase.cs:804` 的 `Mathf.Max(0.55f, …)` 硬下限（改表后会被钳住，换道比直行快）。<br>**T5.5** 佣兵移速：`MercRosterDefs.cs:266-268` + `Mercenary.cs:487` 走 `BASE_MOVE_SPEED`，**确认是否同步减半**（需主人拍板）。 |
| 硬约束 | 禁止只在代码端乘 0.5；代码侧只允许"增加表驱动通道"，数值一律落在表里 |
| 验收 | 玩家/怪同场景实测位移时间 ≈ 翻倍；换道速度 < 直行速度；Boss 移速不被 1.5 倍上限吃掉 |

### T6 · 游侠降攻 + 六职业特征强化（远程不能太轻松）

| 项 | 内容 |
|---|---|
| 负责 | GLM5.3 Flash |
| 现状 DPS（基础攻击 ÷ 攻击间隔） | 剑盾 4.5/0.8=**5.63** · 重武 5.4/1.0=**5.40** · 狂战 3.6/0.7=**5.14** · 游侠 4.8/0.5=**9.60** · 法师 5.1/0.75=**6.80** · 牧师 5.1/0.9=**5.67** |
| 子任务 | **T6.1** 游侠降到中下等（≈剑盾）：建议 `基础攻击 4.8 → 3.0`、`攻击间隔 0.5 → 0.6`（DPS 5.00，略低于剑盾 5.63，远程用射程补）。**具体数值待主人拍板。**<br>**T6.2** 职业特征机制化（备忘已记"多数未做独立玩家机制"）：剑盾=格挡光环（接 `holy_barrier` 常驻版）/ 重武=撞击眩晕 / 狂战=嗜血被动 / 游侠=穿透箭（降攻后补穿透作补偿）/ 法师=元素弹射 / 牧师=治疗光环（`heal_spring` 单体→光环）。<br>**T6.3** 远程限制（三选其二，需主人拍板）：① 移动射击惩罚（移动中攻速/伤害打折，站桩回满）；② 贴身惩罚（近战怪进入 X 距离内，远程单位命中/攻速下降）；③ 远程怪压制（在 `wave_archetype` 的 `ranged` 编成里提高远程怪占比，直接打游侠站位）。 |
| 改动文件 | `Assets/Data/Source/Tables/player_job_base_stats.csv`（数值）、`Config/PlayerJobBaseStats.cs`（若新增列）、职业机制落 `Unit/Hero.cs` / `Combat/` |
| 硬约束 | 攻击间隔 ≥ 0.55（动画锁）；改表后 Cook |

### T7 · 波次与 Boss 形式多样化（治疲劳）

| 项 | 内容 |
|---|---|
| 负责 | 需求拆解 → K3；实现 → GLM5.3 Flash |
| 现状 | `stage_mode.csv`（M01–M10，含 4+/7+ 解锁）+ `wave_archetype.csv`（A01–A08）已是 V3.0 随机波次；**但**每关结构是恒定的「N 波小怪 → Boss」，`SetupBossWave` 不走 stage_mode 表 → 单关骨架十年不变，这是疲劳主因 |
| 子任务 | **T7.1** 引入**关卡事件层**（新增 `stage_events.csv` 或在 `stage_mode` 加 `eventPool` 列）：每关在波间插入 1 个事件，类型建议 —— 伏击（提前埋伏的一波）/ 护送·守卫目标 / 生存计时（撑 N 秒）/ 精英单挑 / 宝箱取舍（开箱触发额外波）/ 商人中途出现。<br>**T7.2** Boss 关差异化：给 Boss 关配 `boss_intro` 变体（Boss 带随从进场 / Boss 分三阶段 / 双 Boss 小队 / 环境机制关：毒区·落石·涨潮），不要全是"小怪波 + 单体 Boss"。<br>**T7.3** 波次节奏差异化：让 `waveMin/waveMax` 在模式间真正拉开（现在基本 3–6），做"少波高密"和"多波低密"两种骨架。<br>**T7.4** 每章给 1 个**专属记忆点**（例：3 章藤蔓会缠住、6 章沉座每次搏动改变地形、8 章雪地脚印减速），避免"换皮不换体验"。 |
| 硬约束 | 全部表驱动；不改 `WavePlanner` 的回退安全网（缺表不能开不起来）；`stage_spawn.csv` 通配行 `monsterTotal` 必须保持 0 |
| 验收 | 连打 10 关，不出现"骨架重复"的主观疲劳；Boss 关至少有 3 种不同开场结构 |

### T8 · 《英雄没有闪》理解纠正 + 局内成长借鉴

| 项 | 内容 |
|---|---|
| 负责 | K3（本条为认知修正，需改既有文档） |
| 修正 | `Docs/英雄没有闪_玩法借鉴分析_2026-09-18.md` 现按"放置挂机 + 暗黑刷宝（墨麒麟那款）"拆解，**方向错了**。用户口径：它是**抽卡式局内成长** —— 打怪掉**金币 + 钻石** → 按顺序**抽卡** → **升级 / 升阶**，表现上像随机，实际是确定性卡序；成长爽感不错。 |
| 借鉴点 | ① 局内成长从"三选一"扩到"资源驱动的顺序卡序"（金币走升级、钻石走升阶），给玩家"我在攒钱买下一个确定提升"的目标感；② 现有基建可复用：`Systems/ResourceWallet.cs`（金币/钻石）、`Systems/RunLoadout.cs`（星级）、`Systems/BattleRunStats.cs`、势能系统。 |
| **明确不借鉴** | 它的「每波小怪 → 最后 Boss」单一骨架 —— 正是 T7 要解决的疲劳源。 |
| 待办 | 重写该分析文档；是否落地"局内抽卡成长"需主人拍板（会改变单局时长与体力曲线） |

---

## 二、执行顺序建议

```
T4（遮罩半透）→ T3（文字动效）→ T2（章节两段式文案）→ T1（Loading 文案）
   ↓ 独立并行
T5（移速减半 + 补表驱动通道）→ T6（游侠降攻 + 职业机制）
   ↓
T8（认知修正，先改文档）→ T7（波次多样化，工作量最大，最后做）
```

## 三、需要主人拍板的 5 件事

1. 游侠最终数值（T6.1 建议 攻 3.0 / 间隔 0.6，DPS 5.0）
2. 佣兵移速是否同步减半（T5.5）
3. 远程限制三选其二（T6.3）
4. 关卡事件层是新增表还是 `stage_mode` 加列（T7.1）
5. 是否落地"局内抽卡成长"（T8）—— 会改变单局时长
