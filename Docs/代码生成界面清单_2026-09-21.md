# 代码生成界面清单（2026-09-21）

> 口径：这些界面**没有预制体**，全部由 C# 在运行时 `new GameObject` + `AddComponent` 拼出来。
> 本文按「玩家从开机到通关」的实际出现顺序排列，方便对照排查。
> 文件:行为准，行号基于 2026-09-21 的代码状态。

---

## 零、已确认的死代码

| 界面 | 原设计用途 | 现状 |
|---|---|---|
| `PausePanel`（战斗暂停菜单） | 继续 / 设置 / 撤离 / 超时倒计时 | 🚨 **永远不会出现**。无预制体、无 AddComponent、无 Resources.Load；全工程只在 `AutoGameInitializer.cs:392` 和 `BattleUI.KillCam.cs:18` 两个「隐藏节点名单」的字符串里出现。**主人已确认：战斗中的暂停就是设置菜单，打开时游戏暂停、关闭即恢复。** 此文件等主人决定是否删除。 |

---

## 一、开机 → 登录

| 大白话名字 | 类名 | 什么时候出现 | 触发点 |
|---|---|---|---|
| 工作室 Logo 动画 | `StudioLogoSplash` | 一开游戏就播，插在健康忠告**之前**，约 3.6 秒，**不可跳过** | `BootManager.cs:55` |
| 登录前开场视频 | `LoginIntroSplash` | Logo 播完后、登录界面出来前；**点任意处可跳过** | `BootManager.cs:57` |
| 登录页 LOGO 呼吸动效 | `LoginLogoMotion` | 登录界面显示期间一直跑（入场、浮动、扫光、像素星点） | `LoginUI.cs:338` |
| 按钮按下变暗回弹 | `UiButtonPressFeedback` | 进任何场景时，自动给 canvas 下所有按钮挂上（不是单独界面） | `UICanvasSetup.cs:42` |
| 预制体位置保护 | `UiPrefabRectGuard` | 战斗 / 登录界面启动时，防止美术摆好的底图被脚本改歪 | `BattleUI.cs:120`、`LoginUI.cs:158` |
| 刘海屏 / 手势条适配 | `SafeAreaFitter` | 底部导航栏、顶部任务条创建时自动挂 | `MainBottomNav.cs:64`、`QuestHudBar.cs:217` |

---

## 二、进城镇

| 大白话名字 | 类名 | 什么时候出现 | 触发点 |
|---|---|---|---|
| 城镇场景启动器 | `TownSceneManager` + **`TownSceneBootstrap`** | 载入 Town 场景的一瞬间自动建；负责预热字体、预实例化各功能页（隐藏），保证点底栏不卡 | `TownSceneManager.cs:50`、`TownSceneBootstrap.cs:32` |
| 城镇页签切换器 | `TownHubController` | 进城镇就挂上，之后点底栏 5 个入口全靠它切页 | `GuildHallUI.cs:75` |
| 城镇黑幕 | `TownIntroVeil` | 刚进城镇、片头剧情播放期间盖住空场景，避免露馅 | `GuildHallUI.cs:84`、`TutorialDirector.cs:188` |
| 片头视频遮罩 | `OpeningIntroOverlay` | **首次**进城镇播放片头视频，可点击跳过 | `TutorialDirector.cs:309` |
| 首次启动强制引导 | `FirstRunGuide` | 新号第一次玩：一步步指引冒险 / 天赋 / 酒馆 / 首战 | `TutorialDirector.cs:51` |
| 看板娘对话框气泡 | `SpeechBubbleTalker` | 城镇公会页常驻，打字机效果轮换台词 | `GuildHallUI.cs:123` |
| 红点提醒 | `RedDot` / `UIRedDot` | 有新邮件、可领奖励、图鉴有未看内容时亮 | `GuildHallUI.cs:199` 起批量绑定 |

---

## 三、城镇 · 顶部与页签

| 大白话名字 | 类名 | 什么时候出现 | 触发点 |
|---|---|---|---|
| 主界面「当前目标」条 | `QuestHudBar` | 城镇顶部常驻，显示当前主线/每日目标 | `TownSceneBootstrap.cs:124` |
| 任务面板（全部任务） | `QuestPanelUI` | 点任务条右侧「**全部**」展开，主线 / 每日两个分页 | `QuestHudBar.cs:646` |
| 城镇商店 | `ShopUI` | 公会大厅点「**商店**」 | `GuildHallUI.cs:360` |
| 日志里程等级奖励 | `AchievementMilestoneUI` | 公会大厅点「**执照厅**」 | `GuildHallUI.cs:407` |
| 冒险日志三期面板 | `AdventureLogPhase3Panel` | 打开冒险日志页（碎片合成 / 里程商店） | `AdventureLogUI.cs:104` |

---

## 四、城镇 · 角色页

| 大白话名字 | 类名 | 什么时候出现 | 触发点 |
|---|---|---|---|
| 独立背包弹窗 | `BackpackPopupUI` | 角色页点「**背包**」 | `CharacterUI.cs:527` |
| 看板（已解锁佣兵）三选一 | `BoardSelectPopupUI` | 角色页点「**看板**」 | `CharacterUI.cs:560` |
| 技能选择弹层 | `SkillSelectUI` | 角色页点「**技能**」（6 个技能格） | `CharacterUI.cs:494` |
| 背包格子网格 | `TownBackpackGrid` | 角色页 / 背包弹窗里铺格子（只绑已摆好的格子） | `CharacterUI.cs:697`、`BackpackPopupUI.cs:157` |
| 换装离屏预览 | `TownHeroCostumePreview` | 角色页打开或换装备时，离屏渲染角色形象 | `CharacterUI.cs:117` |
| 立绘呼吸动效 | `PortraitIdleMotion` | 任何立绘显示时（角色页 / 酒馆 / 剧情对话 / 招募弹窗） | `CharacterUI.cs:596`、`DialogueUI.cs:1117` 等多处 |

---

## 五、城镇 · 酒馆

| 大白话名字 | 类名 | 什么时候出现 | 触发点 |
|---|---|---|---|
| 酒馆三选一招募 | `TavernRosterPanel` | 酒馆招募时，从形象池三选一 | 自身 `TavernRosterPanel.cs:36` 单例 |
| 老板娘立绘彩蛋 | `TavernLandladyTease` | 点酒馆老板娘立绘；哆嗦 + 警告，**点满 10 次被踢出约 1 分钟** | `TavernUI.cs:501` |
| 酒馆入口被踢倒计时 | `TavernNavBanHud` | 被老板娘踢出期间，底栏酒馆图标变暗 + 倒计时，点了弹气泡 | `TownHubController.cs:64` |
| 独眼情报商问答 | `InformantQuizDirector` | 酒馆找情报商做日常情报 / 剧情问答 | 自身 `InformantQuizDirector.cs:21` |

---

## 六、战斗

| 大白话名字 | 类名 | 什么时候出现 | 触发点 |
|---|---|---|---|
| 章节开场标题黑屏 | `ChapterSplashOverlay` | 进战斗时先黑屏显示章节标题，停留后渐隐自毁 | `BattleManager.cs:1131` |
| 虚拟摇杆 | `BattleJoystick` | 战斗全程，点在下方装备栏区域出现；结算 / 拾取时隐藏 | `BattleUI.Backpack.cs:362` |
| 右侧击破 + 下一波倒计时 | `BattleSideHud` | 战斗全程右侧常驻，倒计时可点击加速出兵换金币 | `BattleUI.cs:107` |
| Boss 血条 | `BattleBossHpBar` | 遇到 Boss 或精英波时出现在屏幕上方 | `BattleUI.cs:110` |
| 波次预告横幅 | `BattleWaveAnnounceUI` | 每波之间砸入「**下一波来袭**」/「**Boss 来袭**」 | `BattleManager.cs:1416` |
| 头顶气泡对话 | `BattleHeadTalkUI` | 佣兵阵亡台词、教程对话；点一下跳过打字 | `BattleManager.cs:626`、`MercBattleBanter.cs:55` |
| 局内构筑 HUD | `RunSkillBarUI` | 开局起常驻，显示本局战力 + 已获技能 + 出战佣兵 | `RunDraftDirector.cs:63` |
| 局内抽卡三层 | `LevelUpDraftUI` | 升级 / 关卡结算时：先选方向（技能/佣兵/装备）再三选一 | `StageClearRewardDirector.cs:814` |
| 技能顺序拖拽小牌 | `SkillOrderChip` | 抽卡界面和战斗技能条上，横向拖动决定技能释放优先级 | `LevelUpDraftUI.cs:407` |
| 低血屏幕四周红闪 | `LowHpScreenEdgeFlash` | 玩家血量低时，屏幕四边脉冲红光（中心镂空，不挡点击） | `Hero.cs:266` |
| 战斗飘字 | `DamageTextSystem` | 每次受击 / 治疗 / 闪避 / 暴击，世界空间飘字 | `UnitBase.cs:1221` |
| 锻造 / 附魔关弹窗 | `CraftStagePopupUI` | 进入锻造关或附魔关时 | `BattleManager.cs:2549` / `:2558` |

---

## 七、结算 & 全局

| 大白话名字 | 类名 | 什么时候出现 | 触发点 |
|---|---|---|---|
| 通关三选一装备卡 | `StageClearEquipUI` | 打完一关，三张装备卡并排，选中后下方显示对比 | `UIManager.cs:90` |
| 全局提示浮字 | `GlobalToastUI` | **哪儿都有**：购买结果、任务完成、连杀 x5/x10/x20、金币不足、战斗中断结算…… | `UIManager.cs:31` 及 40+ 处 |

---

## 八、⚠️ 已停用，现在不会再弹

| 大白话名字 | 类名 | 说明 |
|---|---|---|
| 离线收益弹窗 | `OfflineRewardPopup` | 2026-09-20 起离线金币功能整体暂停，进城镇的自动结算/弹窗入口已停（见 `OfflineGoldCalc.cs:6`）。现在只剩教程里一句 `HideIfOpen()` 兜底。 |

---

## 九、纯辅助件（不是界面，无需预制体）

`GridCellUI`（格子数据）、`MainNavTab`（底栏 5 入口枚举）、`RuntimeUiArt`（运行时画的圆环/圆盘占位图）、`UiBannerPopAnim`、`UiLayoutStretch`、`UiKeyedBackgrounds`、`RuntimeLockSprite`、`SpeechBubbleFit` —— 都是给上面的界面打杂的小组件，保持代码生成是对的。

---

## 附：统计

- 无预制体的界面类共 **44 个**
- 其中**真正值得做成预制体**（整屏手搓 ≥5 个节点）：13 个
  （`BackpackPopupUI` 16 节点最重，其次 `BoardSelectPopupUI` 9、`OfflineRewardPopup`/`TavernRosterPanel`/`TownSceneManager` 各 8）
- 其中**纯代码生成反而更合适**的轻量挂件：约 25 个
- 死代码：1 个（`PausePanel`）
- 已停用：1 个（`OfflineRewardPopup`）
