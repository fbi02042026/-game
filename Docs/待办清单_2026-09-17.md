# 待办 · 需要确认或需要你操作的事

> 日期：2026-09-17
> 范围：佣兵养成美术接入（徽记 / 本命碎片底版）+ 酒馆解锁改造
> 用途：把「还需要拍板」和「需要你在 Unity 里做」的事集中一处，免得散在对话里。

---

## 0. 已经做完的（无需操作）

| 项 | 状态 |
|---|---|
| 徽记 / 碎片底版落 `Resources` | ✅ `Resources/Icons/JobBadge` 18 张、`Resources/Icons/MercFragmentBase` 3 张。导入设置 = Sprite / Single / nPOT=None / mipmap off / alphaIsTransparency / FullRect |
| 佣兵头像立绘同步 | ✅ `Resources/Icons/MercHead` / `MercStand` 已与 `Art` 对齐（代码按**路径** Load，guid 变化不影响） |
| 断链自检 | ✅ `Tools/CheckDanglingRefs.py --strict`：**无新增断链**。脚本类 1 个（已知误报）、资源类 10 个（既有风险清单）、形态非法 `.meta` = 0 |

---

## 1. 需要你在 Unity 里看一眼（验证类）

菜单：**`Tools/UI/预览佣兵养成素材（徽记/碎片底版）`**

要确认三件事：

1. **徽记 18 张是否全命中** —— 预览板底部会写「命中 N / 缺 N」。
2. **碎片卡的 Mask 裁剪对不对** —— 头像是按**底版图自身的 alpha** 裁的。
3. **层序** —— `MercGrowUI.FrameOnTop` 默认 `true`（底版压在头像上层，底版纹样一定看得见）。
   预览板底部专门放了一张 `FrameOnTop=false` 的对照卡。你看哪张视觉对，说一声我把默认值翻过来
   （改 `Assets/Scripts/UI/MercGrowUI.cs` 第 17 行，一行）。

---

## 2. 需要拍板的（4 条）

| # | 问题 | 位置 | 建议 |
|---|---|---|---|
| ~~Q1~~ ✅ 已定 | 商店定位文案 | `Assets/Scripts/UI/ShopUI.cs` | **整句去掉**（2026-09-18）。「不卖独占内容」与「7 佣兵只能商店解锁」冲突，Hint 节点不再创建 |
| ~~Q2~~ ✅ 已定 | 佣兵详情弹窗加「解锁」按钮 | `CodexInfoPopupUI.cs` + `TavernUnlockUI.cs` | **加**（2026-09-18）。运行时补 `UnlockButton` 节点，**不动预制体**；未解锁且门槛已开才显示 |
| ~~Q3~~ ✅ 已定 | 本名碎片瓶颈 | `Docs/佣兵养成_随机化与抗性_2026-09-17.md` §10 | **10 周目**（2026-09-18）。公式 `1 + floor((n-5)/8)` → **`/10`** |
| Q4 | 狂战主手档位仍是 `Greatsword`（攻速 0.65） | `Assets/Scripts/Config/PlayerJobDefs.cs` 第 74 行附近 | 改 `Sword` 才有双剑快攻手感，但会**连带改狂战的武器掉落池**（`CanJobDropWeapon`），所以没有私自改 |

> Q1 / Q2 / Q3 出自 `Docs/酒馆与佣兵养成_合并落地清单_2026-09-17.md` §5；Q4 出自 2026-09-17 的修复记录。

---

## 3. 需要点位置：徽记 / 碎片卡挂到哪个界面

**碎片卡**（底版 + 头像 Mask）候选落点：

- 酒馆名册卡 `Assets/Scripts/UI/TavernUnlockUI.cs`（已在用 `MercHireSession.LoadRarityFrame` + `MercPortraitSprites.GetHead`）
- 招募弹窗 `Assets/Scripts/UI/MercenaryRecruitPopupUI.cs`
- 商店「本命碎片」商品格 `Assets/Scripts/UI/ShopUI.cs`

**徽记**（6 职业 × 普通/稀有/传奇）候选落点：

- 名册卡角标 / 佣兵详情弹窗 / 升级面板

> ⚠️ **依赖前提**：项目目前**没有佣兵碎片系统**（只有技能碎片 `SkillFragmentSystem`）。
> `mercFragments` / `jobBadges` 属于 `Docs/佣兵养成_随机化与抗性_2026-09-17.md` §9 里 **`SaveData` 待加的 8 个字段**，
> 还没有落。所以要接「买重复佣兵 → 转本命碎片」这条线，得先落存档字段。

---

## 4. 已确认不用管的（免得反复被提醒）

- `Assets/Resources/Prefabs/UI/WorldMapPopup.prefab` 的 `1340940d…` 脚本断链 = **已知误报**，Unity 里看不到 Missing Script。
- 资源类断链 10 个 = 既有潜在风险清单（只是磁盘表里没 guid，**不等于 Unity 现在缺图**）。
- 根目录 0 字节误建文件已清理。

---

## 附：相关文件索引

| 用途 | 文件 |
|---|---|
| 素材加载器 | `Assets/Scripts/UI/MercGrowSprites.cs` |
| UI 构件（碎片卡 / 徽记角标） | `Assets/Scripts/UI/MercGrowUI.cs` |
| 同步 + 预览菜单 | `Assets/Editor/MercGrowArtTool.cs` |
| 资源路径常量 | `Assets/Scripts/Data/ContentPaths.cs`（`Icons.JobBadge` / `Icons.MercFragmentBase`） |
| v4 养成设计 | `Docs/佣兵养成_随机化与抗性_2026-09-17.md` |
| 合并落地清单 | `Docs/酒馆与佣兵养成_合并落地清单_2026-09-17.md` |
