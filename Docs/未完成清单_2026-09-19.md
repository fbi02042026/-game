# 未完成清单（2026-09-19 20:45 整理）

> 主人要求「重新列一下还没改的」。本文档按 **数据导向** 排序（主人核心指标：**在线时长 → 留存 → 后期广告变现**）。
> 已完成的见 `Docs/天赋右列重制方案_2026-09-19.md` §12~§15。

---

## 一、待主人拍板（阻塞后续）

| # | 事项 | 现状 | 建议 |
|---|---|---|---|
| 1 | **钻石产出偏紧** | 新增 3 个钻石消耗口（体力20/金币20/刷新50），现有日均产出仅 10~20 钻（每日登录+成就+图鉴+里程） | 建议每日任务/章节首通补钻，或先降消耗口价格 |
| 2 | **金币加号性价比差** | 20 钻换 20 金 | 降到 10 钻，或奖励提到 40+ 金 |
| 3 | **聚光灯包里遗产刷新变付费** | 原本 Spotlight 包免费刷一次，改钻石后也要付费 | 若不合适，给 Spotlight 加免费分支 |
| 4 | **锁图标没用真图** | `Assets/Resources/UI/Common/锁.png` 的 `.meta` 是 `textureType:0`（Default 非 Sprite），`Resources.Load<Sprite>` 返回 null，当前走**代码画像素锁**兜底（功能正常） | 在 Unity 里把该图改 **Sprite (2D and UI) / Single** 并 Reimport，自动切真图，无需改代码 |
| 5 | **正式文案** | 老板娘对话、C1 剧情、主线任务标题均为占位（已标 `待主人补正式文案`） | 主人补，替换引号内字符串即可 |
| 6 | **第 3~8 章主线任务配满** | 目前只配了第 1、2 章样例（各 4 条） | 每章 3~4 条，至少 1 条非战斗 |
| 7 | **战前剧情重播策略** | 现在每章第一关都重播 | 若要「每周目只播一次」需加存档位（未改 SaveData） |
| 8 | **离线金币改造** | 见下文 §三 | 建议改「可加速」 |

---

## 二、离线金币（主人问：放哪/入口/怎么领/是否必要/能否改）

| 项 | 现状 |
|---|---|
| 存放 | `Assets/Scripts/Systems/OfflineGoldCalc.cs`（计算）+ `Assets/Scripts/UI/OfflineRewardPopup.cs`（弹窗） |
| 触发 | `Assets/Scripts/UI/TownSceneBootstrap.cs:186` —— **进城镇场景时自动弹** |
| 入口 | **没有主动入口**，纯被动弹窗 |
| 领取 | 自动到账（按离线秒数 × 农场等级，有 `maxMin` 上限） |
| 是否有必要 | **有必要**——它是低成本的**次日留存钩子**（"回来有钱拿"），删了会掉留存 |
| 能否改成别的 | 能。建议方向（数据导向）： |

**改造建议（把"给钱"变成"消耗位/期待感"）**：
1. 主界面加**主动入口**（显示"离线收益已累计 X"），变被动弹窗为主动领取 → 提升点击与期待；
2. 支持**加速/翻倍**：钻石翻倍（现在）→ 后期看广告翻倍（直接服务广告指标）；
3. 收益除金币外，少量给**天赋石/道具**需谨慎（会冲击刚定的 536 石/周目产出模型，需重算）。

---

## 三、数据指标（主人强调：做什么都要看数据）

主人明确的指标：**在线时长、留存、后期看广告**。
女仆补充建议关注的（按手游常规）：

| 指标 | 为什么 | 当前埋点状态 |
|---|---|---|
| 在线时长 / 单次会话时长 | 主人核心 | ⚠️ **未见埋点系统**（R14 广告埋点也未做） |
| 次日/7日留存 | 主人核心 | ⚠️ 同上 |
| 关卡进度分布（卡在第几章） | 找流失点、调难度 | ❌ 无 |
| 关卡通关率/失败率 | 难度曲线是否合理 | ❌ 无 |
| 钻石产出/消耗比 | 经济是否健康 | ❌ 无 |
| 广告/钻石点击率（后期） | 变现阶段 | ❌ 无（广告当前已关闭，走钻石） |

**建议**：补一个轻量埋点层（会话开始/结束、关卡开始/结算、章节进度、钻石收支），
这是"用数据指导改什么"的前提。**这块还没派工，等主人确认埋点方案/平台（是否用第三方 SDK）。**

---

## 四、未派工的功能（按数据价值排序）

### P0（直接影响留存/在线时长，建议优先）
| 项 | 内容 | 数据价值 |
|---|---|---|
| **新手引导任务** | MainQuest 框架已建，第 1 章已有非战斗目标；可补"第一次升天赋/招佣兵/分解装备" | 降低新手流失 |
| **引导文案两段式**（已做战前） | 章节战斗前 2 行 → 首波前 2-4 句对话 | 已派工完成 |
| **剧情/世界观补完** | 老板娘互动、艾丽娅过往（已占位） | 留存/沉浸感 |

### P1（玩法深度，防中期疲劳）
| 项 | 内容 |
|---|---|
| **波次多样化** | 现在关卡骨架恒定（N 波小怪→Boss，`SetupBossWave` 不走 stage_mode 表）→ 加**关卡事件层**与 **Boss 关变体** |
| **游侠降攻 + 职业特征强化** | 游侠 9.60 DPS 偏高，要降到中下等；远程不能玩得太容易 |
| **天赋 R14/R15** | 广告埋点（现广告改钻石，需重定义）+ 职业专属特效 6 个 |

### P2（系统完善/技术债）
| 项 | 内容 |
|---|---|
| 硬编码参数迁 `combat_tuning.csv`（P0/P1） | 清理 `monster_unlock_tier` 双轨与死代码 `CHAPTER_SCALE_PER` |
| 洗点二次确认弹窗 | 方案里有要求，未做 |
| 背包第 4 行全场景可见 | 战斗背包/角色页网格来自 prefab（12 格=3 行），需主人放开 prefab 或运行时补格 |
| 佣兵碎片存档字段 | 待办 E |
| 徽记/碎片卡挂点 | 待办 F |
| SafeArea 刘海适配 | 方案已出未实施 |
| `AdventureUI.cs:364` 写死 `mapH=1280` | 待办 C |
| 宽屏左右黑边 | 待办 B |
| 排查其他"代码生成 UI"的 prefab 残留 | 待办 G |

### P3（主人已定：主玩法做精后再做）
| 项 | 内容 |
|---|---|
| **副本玩法** | 主人明确「优先把主玩法做好做精后再开始」→ 暂缓，等主玩法验收完再议 |
| 其它玩法扩展 | 同上 |

---

## 五、本轮（20:00-20:45）已完成

| 组 | 内容 | 状态 |
|---|---|---|
| 删除 MercBirthFragment | 78 张合成图 + Editor 生成器 | ✅ 已删 |
| 编译错误修复 | `TalentDefs` CS1620×2（TryInt 数组重载）、CS7036×6（HNode 补 id R_C1~R_C6）、`MercGrowInventory` CS0246（`GridBackpackSystem.BackpackItem`） | ✅ |
| 战前剧情接线 | `CoPreBattleStoryThenFirstWave` 之前**无调用点**（等于没生效）→ 已接进 `BattleStartSequenceCoroutine:1152`；补中断复位；异常时直接放行首波 | ✅ |
| 背包未解锁格子 | 半透遮罩 + 锁图标 + 不可点击（复用现成图，缺图已登记） | ✅ |
| 广告 → 钻石 | 3 处广告点（体力/金币/遗产刷新）全部改钻石；`GameConfig.ADS_ENABLED_BEFORE_SPOTLIGHT = false` 开关；项目**已有钻石体系**直接复用 | ✅ |
| 主界面任务条 | 新建 `QuestHudBar`（底部导航上方）+ `QuestPanelUI`（主线/每日两分页） | ✅ |
| 主线多样化 | 新建 `MainQuestDefs` / `MainQuestSystem` / `MainQuestDialogues`；支持 ClearStage/TalkNpc/WatchStory/RecruitMerc/UpgradeTalent；第 1、2 章已配样例 | ✅ **Unity 实际编译通过，0 error** |

> 未 commit。

---

## 六、2026-09-19 22:50 追加（本轮新完成 + 未做表最终版）

### 6.1 本轮新完成
| 项 | 内容 |
|---|---|
| 任务条移到**主界面顶部** | 主人纠正：不要放底部导航区。现贴顶、避开刘海（`Screen.safeArea` + TopBar 实测底边，兜底 120），半透明底衬不挡视野 |
| **点击任务直达功能** | 新增 `QuestJumpRouter`：打关→冒险页+定位章节 / 找老板娘→酒馆+对话 / 剧情→就地播放 / 招募→酒馆名册 / 天赋→角色页+天赋；跳转失败回退展开面板+提示（不会"点了没反应"） |
| **首次强制软引导** | 新增 `FirstRunGuide`：6 步（欢迎→冒险→首战→天赋→任务条→酒馆老板娘）；高亮圈+半透遮罩(0.45)+气泡；PlayerPrefs 存游标，中途退出续做；无"跳过全部"（每步 45~90s 超时兜底放行） |

### 6.2 未做表（明天照这个推进）

| 优先级 | 事项 | 提升哪个数据 | 状态 |
|---|---|---|---|
| **P0-1** | **埋点系统**（会话时长/留存/关卡进度/钻石收支） | 主人核心指标，没有它无法指导改什么 | ⚠️ 无 |
| **P0-2** | 第 3~8 章主线任务配满（现只有 1、2 章样例） | 在线时长、中期留存 | 待配 |
| **P0-3** | 正式文案（老板娘对话/C1 剧情/任务标题，现为占位） | 沉浸感→留存 | 待主人补 |
| **P0-4** | **钻石产出补充**（现日均 10~20 钻，消耗口已开 3 个→净流出） | 经济健康、后期广告位 | 待定 |
| **P0-5** | 首次引导顺序确认（现任务条排第 5 步，主人原意第 2 步） | 新手流失 | 待拍板 |
| **P1-1** | 波次多样化（关卡骨架恒定→中期疲劳） | 留存 | 未做 |
| **P1-2** | 游侠降攻 + 职业特征强化 | 平衡 | 未做 |
| **P1-3** | 天赋 R14/R15（广告埋点需重定义 + 职业特效 6 个） | — | 未做 |
| **P1-4** | 离线金币改造（加主动入口 + 钻石/广告翻倍） | 次日留存、后期广告 | 待拍板 |
| **P2-1** | 硬编码迁 `combat_tuning.csv`；清 `monster_unlock_tier` 双轨与 `CHAPTER_SCALE_PER` 死代码 | 技术债 | 未做 |
| **P2-2** | 洗点二次确认弹窗 | — | 未做 |
| **P2-3** | 背包第 4 行全场景可见（prefab 12 格限制） | — | 需主人放开 prefab |
| **P2-4** | 佣兵碎片存档字段、徽记/碎片卡挂点 | — | 未做 |
| **P2-5** | SafeArea 刘海适配、`AdventureUI:364` 写死 mapH=1280、宽屏黑边 | — | 未做 |
| **P2-6** | 排查其他"代码生成 UI"的 prefab 残留 | — | 未做 |
| **P2-7** | `锁.png` 改 Sprite(2D and UI) 后 Reimport（现走代码画像素锁兜底） | — | 需 Unity 操作 |
| **P3** | **副本玩法 / 其它玩法扩展** | — | 主人已定：主玩法做精后再做 |

### 6.3 Unity 侧待验证
1. 编译确认（子代理无 Unity Editor，只做了符号/配平自查）
2. 换 guid 后 `TavernUnlockUI` 脚本与 H001 头像是否正常挂载
3. 移速减半手感实测（预期位移时间×2）
4. 顶部任务条在刘海机的实际位置
