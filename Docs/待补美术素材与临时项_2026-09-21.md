# 待补美术素材 + 临时/占位项 总清单

> 日期：2026-09-21（下午）｜ 触发：主人说「把所有的临时的东西都整理出来」「如果缺美术素材的给我一个表」
> 配套：`Docs/剧情插画需求评估_位置_尺寸_提示词.md`（背景尺寸/提示词原文）、`.workbuddy/memory/REF-规格与数值.md`（美术规格速查）

---

## 〇、先报一件要紧的：**4 张结局背景其实早就画好了，但一直没接进游戏**

| 项 | 情况 |
|---|---|
| 图在哪 | `Assets/Art/UI/Story/end_happy.png` / `end_badhome.png` / `end_badgate.png` / `end_true.png` |
| 尺寸对不对 | **完全正确**，4 张都是 **576×1024**（和 `bg_guild_office.png` 一模一样） |
| 问题 | ① 运行时目录 `Assets/Resources/Story/Backgrounds/` 里**没有它们**（只有公会大厅/办公室 2 张）<br>② 全工程 **0 处代码引用**它们 |
| 后果 | 打到第 8 章看结局时，画面上只有**战斗背景（雪原卷轴）+ 立绘**，这 4 张图白画了 |
| 本轮处理 | **已接上**（见 §五「这次动了什么」）。你没让我接的话，说一声我回退，3 处改动很好撤。 |

> 为什么以前没发现：探针只查「引用了但文件不在」的断链，**查不出「文件在但没人引用」**。
> 这是两类不同的问题，我已经在 §六 把探针补了一条对应能力。

---

## 一、缺美术素材表（这些是**真的还没有图**）

### A. 剧情背景（规格：**576×1024**，9:16，不要透明底）

| # | ID | 出现在哪 | 画面 | 优先级 | 提示词 |
|---|---|---|---|---|---|
| ~~1~~ | ~~`end_badhome`~~ | N8-A《回来的不是她》 | 公会医务室清晨，空病床 | ✅ 已出图 | — |
| ~~2~~ | ~~`end_happy`~~ | N8-B《回来的是两个人》 | 裂隙出口，天刚亮 | ✅ 已出图 | — |
| ~~3~~ | ~~`end_badgate`~~ | N8-C《留下的是她》 | 裂隙最深处，沉座合拢成晶体 | ✅ 已出图 | — |
| ~~4~~ | ~~`end_true`~~ | N8-T《最后一个箱子》 | 旧装备仓库，开盖的补给箱 | ✅ 已出图 | — |
| 1 | `grave_seven` | N2 第 2 章 Boss 后 | 墓园最里侧，七块无名碑 + 第八块空的 | ✅ **已接入** | 2026-09-21 晚：图放两处 + 注册「无名碑」+ N2 首句挂 `.Bg()` |
| 2 | `abyss_choice` | N7 第 7 章三选一前 | 裂隙深处无底深渊边缘，悬空石台 | ✅ **已接入** | 2026-09-21 晚：图放两处 + 注册「深渊边缘」+ N7 首句挂 `.Bg()` |
| 3 | `wreck_ship` | N5 第 5 章 Boss 后 | **搁浅的补给船**（按 N5 实际台词写） | ✅ **已接入** | 2026-09-21 晚：图放两处 + 注册「搁浅的补给船」+ N5 首句挂 `.Bg()`（**替换掉作废的 `briefing_tent`**） |
| 4 | ~~`briefing_tent`~~ | ~~N5~~ | ~~遗迹营地简报桌~~ | ⛔ **作废** | 提示词写错了：照过时策划写的「帐篷」，但 N5 台词里**根本没有帐篷**。图已生成但**无处可用**，可删或留作备用 |
| 5 | `throne_seat` | N6 第 6 章 Boss 后 | 巨岩深窟底部，黑曜石座静静立着 | ✅ **已接入** | 2026-09-21 复制到 Resources + 注册 ID + N6 首句挂 `.Bg()` |
| 6 | `melissa_camp` | N3 第 3 章（二周目） | 藤蔓后收拾干净的营地，篝火还热 | ✅ **已接入** | 2026-09-21 复制到 Resources + 注册 ID + N3 首句挂 `.Bg()` |
| 7 | `windmill` | N4 第 4 章（二周目） | 草原上的计时风车，刻度停在第七格 | ✅ **已接入** | 2026-09-21 复制到 Resources + 注册 ID + N4 首句挂 `.Bg()` |

> ✅ **2026-09-21 晚更新：3 张全部出图并接入** —— `grave_seven` / `abyss_choice` / `wreck_ship`
> 已放两处目录、注册 ID + 中文地名、首句挂 `.Bg()`。**剧情背景 10 张全部有图、零缺口。**
>
> ~~原来缺这 3 张时的效果：「这三段剧情只有对话框和立绘，背景沿用上一张」~~（已解决）。
> N2（发现真相）、N5（第一次冲击）、N7（做抉择）是全剧三个最重的情感点，现在都各有自己的背景了。
>
> ⚠ **我上一版这里写「✅ 已有」是措辞糊了** —— 那两栏指的是「**提示词**已有」，
> **不是图已经有**。图一直没出，所以才一直"还缺"。抱歉误导。
>
> ⚠ **出完图别直接丢 `Art/UI/Story/`** —— 那只是编辑器源目录。要**同时**放一份到
> `Assets/Resources/Story/Backgrounds/{id}.png`，否则打包后（微信小游戏）读不到。这一步最容易漏，
> 这次的 4 张结局图就是只放了源目录。

#### 附：出图后「接进游戏」的 4 步（照抄即可，说你一声我来做也行）

| 步 | 做什么 |
|---|---|
| 1 | 图放两处：`Assets/Art/UI/Story/`（源）+ `Assets/Resources/Story/Backgrounds/{id}.png`（运行时，**必须有**） |
| 2 | 运行时那份要有 `.meta`：照抄同目录 `guild_hall.png.meta`，**把 `guid` 和 `spriteID` 换成两个全新的 32 位 hex**（不能跟源图那份重复，否则 guid 冲突）。或者跑 `python Tools/EnsureMeta.py --fix` 让它自己补（补齐后在 Unity 里确认 `textureType=8 / spriteMode=1`） |
| 3 | `Scripts/Story/StoryBackgrounds.cs`：加 `public const string Xxx = "id";` + `DisplayName()` 里补中文地名（**不能漏**，漏了换背景时会显示一片空白） |
| 4 | `Scripts/Story/ChapterStoryBeats.cs`：在**那一段的第一句**后面接 `.Bg(StoryBackgrounds.Xxx)`（只挂第一句，重复挂会多播一次地点揭示黑场） |

改完跑一次 `python Tools/EnsureMeta.py` + `python Tools/CheckDanglingRefs.py --strict` 就算完。

### B. 剧情立绘（规格：**1171×1345**，实际只显示上方 72% ⇒ 1171×968）

| # | 角色 | 状态 |
|---|---|---|
| 全部 12 个剧情角色 | 玩家 / 前台 / 会长×2 / 猎人 / 小梅 / 阿尔托 / 劳顿 / 梅莉莎 / 老板娘 / 灰 / **裂缝意志** | **11 个已到位**；「裂缝意志」**已定案不出图** —— 结局改成黑屏 + 一句话（见 §四） |

> 具体核实：`hunter→C004`、`xiaomei→C001`、`altor→C002`、`grey→C003`、`laodun→H001`、
> `innkeeper→receptionist`（老板娘复用前台，美术已确认不另出图）—— 全部能解析到真图，**没有隐藏缺口**。

### C. 图标类

| 类别 | 现状 |
|---|---|
| 道具图标 `Resources/Icons/Item` | 8 张，**够用**（道具表引用 8 个图标名，6 个可对号入座） |
| 玩家技能图标 `Resources/Icons/SkillIcon` | 6 张 = `player_skills.csv` 6 个技能，**对齐** |
| 佣兵技能图标 `Resources/Icons/MercSkill` | 20 张 = `merc_skills.csv` 20 条，**对齐** |
| 职业徽记 `Resources/Icons/JobBadge` | 18 张 = 6 职业 × 3 档，**齐** |
| 本命碎片底版 `Resources/Icons/MercFragmentBase` | 3 张 = 普通/稀有/传奇，**齐** |
| 佣兵头像/立绘 `MercHead` / `MercStand` | 26 / 31 张，**齐** |
| 战斗背景 `Art/UI/background/{1..8}/` | 8 章 × 3 张，全 **2020×552**，**齐** |
| 剧情小道具 `Resources/Story/Props` | 2 张（纸条、气泡），**齐** |

**结论：图标和立绘没有缺口，还需要你出图的只剩 §一-A 那 3 张背景**（`grave_seven` / `abyss_choice` / `wreck_ship`；裂缝意志已定案不出图）。

---

## 二、不算缺图、但接错了的 1 处（不用出图，改表即可）—— **已修**

| 位置 | 问题 | 影响 |
|---|---|---|
| `Assets/Data/Source/Tables/item_defs.csv` 第 33 行「本命碎片」 | 原图标路径 `Icons/MercBirthFragment/frag_H001_普通` 是**死链**（`MercBirthFragment` 目录已随旧组件删掉，现在走 `MercFragmentBase` 底版 + `JobBadge` 徽记两层方案） | ✅ **已修（2026-09-21）**：该行 `icon` 已清空，`.csv` 与 `item_defs.bytes` 同步落盘（3033 B，两边字节一致） |

> 为什么**留空**而不是填 `Icons/MercFragmentBase/…`：本命碎片图标是**按佣兵 + 档位动态取**的
> （`MercGrowSprites.LoadFragmentBase(稀有度)`，键形如 `frag:{hireId}`），在表里固化一个静态路径反而会误导；
> `ItemDefs.LoadIcon` 遇到空 `icon` 直接返回 null，界面按「无图」处理，不报错。
> （背包「材料」页里本命碎片 / 职业徽记**是带图标的** —— 那是 `BackpackPopupUI` 走 `MercGrowSprites` 现取的，跟这行表数据无关。）

---

## 三、背景提示词（3 张已接入 / 1 张作废 / 3 张待出图，**提示词全在本节，不用翻别的文档**）

> **2026-09-21 晚修正**：`briefing_tent`（简报帐篷）**作废** —— 那个提示词是照过时策划写的，
> 但 N5 的实际台词写的是「搁浅的补给船」，文本里根本没有帐篷。改由 `wreck_ship` 接手（见 §三-2）。
> 已接入的 3 张（`throne_seat` / `melissa_camp` / `windmill`）已复制到 `Resources/Story/Backgrounds/`、
> 在 `StoryBackgrounds` 注册、并在 `ChapterStoryBeats` N3/N4/N6 首句挂了 `.Bg()`。
> **还缺图的 3 张**（`grave_seven` §三-5 / `abyss_choice` §三-6 / `wreck_ship` §三-2）**提示词都在本节**，不用翻别的文档。
>
> ⚠ 上一版这里写「提示词见配套文档 §5⑤/⑥」**指错了文档** —— 那两节其实在
> 《`Docs/剧情插画需求评估_位置_尺寸_提示词.md`》；本文档 §三 已把全文抄过来。

> **通用后缀（每张都加在正文后面）**：
> ```
> painted fantasy game background, oil painting brushwork, cinematic lighting,
> NO characters, no people, wide vertical composition, 9:16
> ```
> ```
> 厚涂幻想游戏背景，油画笔触，电影级光影，画面中不要人物，竖构图 9:16
> ```
>
> **通用负面提示词**：
> ```
> people, character, figure, text, letters, watermark, signature, logo,
> modern objects, vehicles, lowres, blurry, jpeg artifacts, oversaturated, 3d render, photo
> ```
>
> **出图尺寸 576 × 1024**（9:16，铺满不裁不拉伸）。每张都**不要画人物**（人物由立绘叠加，画了会打架）。

### 1 `throne_seat`｜N6 巨岩深窟 · 沉座

```
the bottom of a vast rocky cavern, a single black obsidian throne-like seat standing alone,
faint rhythmic pulse of cold cyan light deep inside the stone, scattered ancient battle debris
around it: a rusted shield, broken arrows, dust motes hanging in still air,
oppressive and silent, deep charcoal and cold cyan palette
```
```
巨岩深窟的最底部，一座孤零零的黑曜石座静静立着，石体深处透出有节奏的冷青色搏动微光，
四周散落着更旧的战斗残骸：一面锈盾、几支断箭，静止的空气中悬着浮尘，
压抑死寂，深炭黑与冷青色调
```

### 2 `wreck_ship`｜N5 海岛遗迹 · 搁浅的补给船 ★待出图（替换作废的 `briefing_tent`）

> 按 N5 实际台词写：搁浅的补给船 / 舱门从里面撬开 / 补给箱码得整整齐齐带编号 / 箱底压着泡白的简报。

```
a wrecked supply ship run aground and tilted among jagged coastal rocks, its cargo hatch pried open from the inside,
neatly stacked supply crates in the hold with faded stencil numbers on their faces,
a water-soaked paper document pinned under one crate, pale grey daylight and shallow reef water,
salt-stained weathered wood, muted grey-blue and driftwood brown palette
```
```
一艘搁浅、歪斜在嶙峋礁石之间的补给船，货舱门被人从里面撬开，
舱内补给箱码得整整齐齐，箱面印着褪色的编号，一张泡得发白的纸压在箱底，
灰白的天光与浅浅的礁石海水，木头带着盐渍与风化痕迹，
低饱和灰蓝与浮木棕色调
```

### 3 `melissa_camp`｜N3 翡翠秘境 · 梅莉莎的营地（二周目）

```
a small tidy camp hidden behind hanging jungle vines, a campfire still glowing with warm embers,
a folded blanket, a writing board and pen resting on a flat stone, path markers strung through
the vines at ankle height, lush green jungle with soft light filtering down,
living warmth against the cold ruins, emerald green and warm orange palette
```
```
垂落的丛林藤蔓后藏着一处收拾得极干净的小营地，篝火还留着暖色余烬，
折叠的毯子、一块石板上的写字板和笔，脚踝高度的藤蔓间拉着几道示警细线，
茂密绿意里透下柔光，在冷寂遗迹里透出活着的气息，翠绿与暖橙色调
```

### 4 `windmill`｜N4 晨曦草原 · 计时风车（二周目）

```
an old stone windmill on an open grassland plain, its blades locked motionless in a strange position,
the wooden hub carved not with totems but with evenly spaced graduation marks,
faint names carved along the axle, one slot near the seventh mark scraped fresh,
overcast grey-green sky, desaturated grass, unsettling stillness
```
```
开阔草原上一座老旧石造风车，叶片诡异地停死在某个角度，
木质轮毂上刻的不是图腾而是等距的刻度，轴上隐约有一列名字，
第七格附近有一道新刮的痕迹，阴沉的灰绿天空，低饱和草色，令人不安的静止感
```

### 5 `grave_seven`｜N2 幽冥墓园 · 无名碑 ★待出图

> 对过的台词（`ChapterStoryBeats.cs` 152–156 行）：
> 「墓园最里侧有一排没有名字的公会墓碑。你数了数，一共七块。」
> 「最老那块刻着『M. 灰鸦小队 记录员』。再往旁边挪一步，还有第八块——空的。」
> 「名字的位置留着。石头却是新的。有人提前把碑做好了。」
> ⇒ 所以画面**必须**是「七块有年代的旧碑 + 第八块**新的、空白的**」，第八块要明显比前七块新。

```
the innermost row of an old graveyard, seven identical weathered guild tombstones without names,
an eighth blank stone beside them with a freshly carved smooth surface,
cold overcast light, dead grass and fog, somber and quiet, desaturated gray-blue palette
```
```
旧墓园最里侧一排，七块一模一样、被风雨侵蚀的无名公会墓碑，
旁边立着第八块空白石碑，表面新刻过还很光滑，
阴天冷光，枯草与薄雾，肃穆安静，低饱和灰蓝色调
```

### 6 `abyss_choice`｜N7 裂隙最深处 · 深渊边缘 ★待出图

> 对过的台词（`ChapterStoryBeats.cs` 253–272 行）：这段是打完第 7 章 Boss、艾丽娅昏倒在你怀里，
> 然后给你三个选项。台词里**没有点明地貌**（只说「最深处」「那个沉座」），
> 所以画面按「裂隙最深处 · 深渊边缘 · 悬空石台 + 身后两条分开的路」来画，正好呼应「三选一」。

```
the edge of a bottomless abyss deep inside the rift, a stone platform jutting out over the void,
two diverging paths behind, violet-cyan light rising from below, dust and debris floating upward,
vertigo-inducing depth, dark violet and cyan palette
```
```
裂隙深处无底深渊的边缘，一块石台探出悬空，身后是两条分开的路，
下方升起紫青色光，碎屑与尘埃向上飘浮，令人眩晕的深度，暗紫与青色调
```

---

## 四、「裂缝意志」立绘：**已定案不出图**（2026-09-21 主人拍板）

### 定案

- **不出立绘** —— 也不铺占位图、不出对话框。
- Happy 结局最后**黑屏收尾**，黑幕上只留一句话：**「……你们以为砸了它，就结束了？」**
- 位置：整段结局的**最后一句**，排在片尾字卡「追上她的人，终于赶上了。」之后，当收尾钩子用。

### 代码怎么落的（本轮已实现）

| 改动 | 文件 |
|---|---|
| `StoryBeat` 新增 `blackScreen` 字段；新增工厂 `StoryDirector.BlackLine(text)` | `Scripts/Story/StoryDirector.cs` |
| `PlayRoutine` 遇 `blackScreen` 走黑屏分支（不铺背景 / 不出立绘 / 点屏幕继续） | 同上 |
| 新增 `DialogueUI.ShowEndBlackLine()` / `ClearEndBlackLine()`：全黑 + 居中字幕，并收掉立绘与对话框 | `Scripts/UI/DialogueUI.cs` |
| `BuildHappy` 删掉 `Solo("裂缝意志", …, StoryPortraits.RiftWill)`，末尾改加 `BlackLine(…)` | `Scripts/Story/ChapterStoryBeats.cs` |
| `StoryPortraits.AwaitingArt` **继续保留** `RiftWill`（已无调用，留着当「这类角色不出图」的口径） | `Scripts/Story/StoryPortraits.cs` |

> 实现取巧点：黑屏直接用已有的 `RevealBlack` 遮罩层（sibling index 1 —— 天然压在立绘**之上**、位于对话框**之下**），
> 字幕复用「地点名」层 `SetLocationCaption`（居中 + 描边，且会被自动抬到黑幕之上）。
> ⇒ **没有新增任何节点、没有动预制体。**

### 如果哪天又想做（提示词仍有效，留着备用）

**方案 A（推荐）· 「艾丽娅的剪影，被黑曜石吃掉了」**
> 逻辑：C 结局里坐进沉座的人会"不再是自己"。让裂缝意志长成**艾丽娅的轮廓**，玩家一眼就懂"下一个就是她"。

```
half-body portrait of a humanoid silhouette that is almost entirely consumed by fractured obsidian,
only a faint echo of a young woman's outline still readable, hair turned into black shards,
thin cold cyan light leaking from every crack in the crystal, no visible face,
tall vertical composition, upper body only, dark background
```
```
半身立绘，一个几乎被碎裂黑曜石完全吞噬的人形剪影，仍能隐约看出年轻女性的轮廓，
头发化成黑色碎片，晶体每道裂缝里渗出细冷青光，看不见脸，竖构图，只到胸，深色背景
```

**方案 B · 抽象非人形（纯深渊意志）**

```
half-body portrait of an abstract rift entity, a towering cluster of black obsidian shards
floating in a loose humanoid arrangement, cold cyan veins pulsing between the fragments,
void and dust swirling inside, no face, no limbs, ominous and vast,
tall vertical composition, dark background
```
```
半身立绘，抽象的裂隙意志，一堆悬浮黑曜石碎片松散地排成人形轮廓，
碎片之间搏动着冷青色脉络，内部翻涌着虚空与尘埃，没有面孔与四肢，压迫而巨大，
竖构图，深色背景
```

> 真要做时（规格 **1171×1345**，构图只保留上方 **72%**，即有效画面 1171×968）：
> 出图后放 `Assets/Art/UI/Icons/佣兵立绘/riftwill.png`（源）+ `Assets/Resources/Icons/MercStand/riftwill.png`（运行时），
> 再把 `StoryPortraits.cs` 里 `AwaitingArt` 的 `RiftWill` 删掉，并把 `ChapterStoryBeats.cs` 的 `BlackLine(…)`
> 换回 `Solo("裂缝意志", "……你们以为砸了它，就结束了？", StoryPortraits.RiftWill)`。**这几步你说一声我来做。**

---

## 五、临时 / 占位 / 调试项 清点（你要的「把所有临时的东西整理出来」）

### A. 代码里的「临时/占位」——上线前必须看一眼的

| # | 项 | 在哪 | 现在什么样 | 上线前要不要处理 |
|---|---|---|---|---|
| 1 | **占位图生成器** | `Scripts/Story/PlaceholderArt.cs`，`Enabled = true` | 缺图时会**自动画一张写着 ID 的灰盒子**顶上（开发期用来一眼看见缺哪张） | 🟡 **建议发版前设 false**。虽然现在图基本齐了，但正式包里留个"缺图就画调试盒"的开关不稳妥。**风险=0**（现在没有它会触发的场景） |
| 2 | **微信数据上报桥** | `Scripts/Platform/WeChatAnalyticsBridge.cs` | 全是 `Debug.Log` 占位 + `TODO`，**微信后台一条数据都收不到** | 🟢 按你定的「先不做」。真接时要加 `.jslib`（`Assets/Plugins/` 目录现在**根本不存在**） |
| 3 | **激励视频广告桥** | `Scripts/Platform/RewardedAdBridge.cs` | `TODO`；**所有调用方本轮已注释** | 🟢 已按聚光灯要求停用，文件保留备用 |
| 4 | **埋点总入口** | `Scripts/Systems/Analytics.cs` | 调用点都接好了，最后一跳是空的 | 🟢 同 #2，一起做 |
| 5 | **道具「使用效果」** | `Systems/GridBackpackSystem.cs:157` + `item_defs.csv` 的 `useEffect` 列 | 表格注释明写「目前只定义格式、尚未实现效果」；点了使用**数量会减，但什么都不发生** | 🔴 **你点名「先留着接口」→ 保持现状，不动**。等道具内容定稿再做 |
| 6 | **过时 TODO** | `UI/BackpackPopupUI.cs:290` | 注释说"等以后新增养成仓库再接" —— 但养成仓库**早就做完在用了** | 🟢 建议顺手删掉这行注释免得后人误会（一行，无风险） |
| 7 | **广告总开关** | `Core/GameConfig.cs`（`ADS_ENABLED_BEFORE_SPOTLIGHT`） | 本轮**已注释停用**，旁边留了"怎么恢复"的说明 | ✅ 已按你要求处理 |
| 8 | **死配置 2 条** | `MERC_REROLL_GEM_COST = 50`、`BattleRunStats.DiamondGained` | 全工程**没有任何地方读取/赋值** | 🟢 已加 TODO 注释未删。想清就说一声 |
| 9 | **道具表示例行** | `item_defs.csv` 第 20/21 行 | 两条被 `#` 注释掉的示例行 | 🟢 无害，留着当写法范例也行 |

### B. 项目里的备份 / 日志残留（会不会被提交）

| 路径 | 状态 |
|---|---|
| `.workbuddy/backup/**`（约 40 个 `.bak`，含 09-16 预制体、09-18 数值、09-20 职业、09-21 遗产整套） | ✅ **`.gitignore` 已忽略**，不会进仓库 |
| `.workbuddy/tmp/`（20 个文件） | ✅ 已忽略 |
| `BattleDiagnose.log`（17KB）、`debug-*.log`（2 个，共 240KB）、`mem-log/` | ✅ 已忽略 |
| `Tools/prefab-analysis/backup/yongbingsuipian.prefab.bak_20260919` | ⚠ **未被忽略、且已在仓库里** —— 一个 `.bak` 文件躺在项目里，建议清掉（你说一声我来删，或加进 `.gitignore`） |
| `Assets/` 与项目根 | ✅ **干净**，没有 `_tmp_*` / `.bak` 残留 |

### C. 我自己的临时脚本

`C:\Users\Administrator\.workbuddy\` 下有约 130 个 `_tmp_*.py` / `_tmp_*.txt`（我这几天写的探针、日志、备份片段）。
**在项目目录外，不影响仓库**。要不要我清掉？说一声就删。

---

## 六、这次动了什么（怎么回退）

### 1. 4 张结局背景接进游戏（新增，可回退）

| 改动 | 内容 |
|---|---|
| 新增 4 个文件 + `.meta` | `Assets/Resources/Story/Backgrounds/{end_happy,end_badhome,end_badgate,end_true}.png`（从 `Art/UI/Story/` 复制，**源文件没动**；`.meta` 用同目录 `guild_hall.png.meta` 当模板换了全新 guid，已查重 0 冲突） |
| `Scripts/Story/StoryBackgrounds.cs` | 新增 4 个 ID 常量 + `DisplayName()` 补 4 个中文地名（「裂隙出口 / 公会医务室 / 沉座 / 装备科仓库」） |
| `Scripts/Story/ChapterStoryBeats.cs` | 4 个结局的**第一句**各挂一次 `.Bg(...)`（只挂第一句 —— 重复挂会多播一次"地点揭示"黑场） |

**回退方法**：删掉 4 张 PNG + `.meta`；把 `StoryBackgrounds.cs` 里那 6 行和 `ChapterStoryBeats.cs` 里 4 处 `.Bg(...)` 去掉。

### 2. 断链探针精确化（你要求的「探针能不能精确一点」）

| 改动 | 内容 |
|---|---|
| `Tools/CheckDanglingRefs.py` | ① **按字段分危害等级**：`m_Script`🔴必修 / `m_Sprite·m_Texture`🟡玩家可见 / `m_Material·m_Shader`🟢低危 / 其它⚪参考<br>② **打印对象位置**（文件:行 + 字段 + 对象块起始行 + GameObject fileID），不用再全文搜 guid<br>③ **豁免清单**机制 |
| 新增 `Tools/dangling_allowlist.txt` | 把你确认「没问题」的那 11 条登记为**已确认无害**，单独一栏显示，**并且不再让 `--strict` 变红** |

**效果对比**：

```
改之前：  脚本类断链 guid 数 : 1        → --strict 退出码 1（每次体检都是红的）
          资源类断链 guid 数 : 10

改之后：  未豁免断链 guid 数   : 0（其中脚本类 0）   → --strict 退出码 0 ✅
          已豁免（确认无害）数 : 11
          形态非法 .meta 数    : 0
```

> 关键在于：**新冒出来的断链不会自动进豁免清单**，所以以后「需要处理的」那一栏只要不是空的，
> 就是真·新问题，不用再人工筛一遍假警报。

### 3. 背包「材料」页加图标（你选的「徽记碎片直接放背包 + 加图标」）

| 改动 | 内容 |
|---|---|
| `Scripts/UI/BackpackPopupUI.cs` | 材料列表新增可选图标位：**本命碎片**按佣兵稀有度取 `MercFragmentBase` 底版；**职业徽记**按 `badge:{职业}:{档位}` 取 `JobBadge`；顺带把 `养成道具·badge:剑盾:传奇` 这种原始 ID **改成大白话「职业徽记·剑盾·传奇」**。没有图标的行**排版完全不变** |

### 4. 本轮之前（同一个未提交批次里）已经做完的

背包第 4 行写进存档、战斗内只开 2 行（第 3 行上锁）、天赋左列「恭喜获得」、引导进度进存档、
删除背包扩容旧编号、广告相关全部注释、删掉「战前遗产」整套残留、裂缝意志不再顶占位图。

---

## 七、自检结果

```
python Tools/EnsureMeta.py             → 没有缺少 .meta 的资源，全部入库 ✓
python Tools/CheckDanglingRefs.py --strict
    未豁免断链 guid 数   : 0（其中脚本类 0）
    已豁免（确认无害）数 : 11
    形态非法 .meta 数    : 0
    退出码 0 ✅
```

> ⚠ 本机**无法编译**（团结引擎没开/不能跑），以上全是静态核对。
> 请在 Unity 里跑一次，重点看：**第 8 章通关结局**会不会正常切到新背景、有没有多余黑场。
