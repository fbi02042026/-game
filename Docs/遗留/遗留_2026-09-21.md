# 遗留问题 · 2026-09-21（当天快照）

> **规矩（2026-09-21 主人定的）**：遗留问题**不再往一个总清单里堆**。
> 每天一份 `Docs/遗留/遗留_YYYY-MM-DD.md`，只写**当天仍然没解决**的；
> 昨天那份原地归档留档，不再往回改。更早的总清单见 `归档_总清单_截至2026-09-21.md`。

---

## 一、今天做完的（第四批）

| # | 做了什么 | 效果 |
|---|---|---|
| 1 | **3 张新剧情背景接入** | `throne_seat`(N6) / `melissa_camp`(N3) / `windmill`(N4) 已复制到 `Resources/Story/Backgrounds/` + 注册中文地名 + 首句挂 `.Bg()` |
| 2 | **`briefing_tent` 作废 → 改 `wreck_ship`** | 见下面「⚠ 我的错」；N5 背景暂时不挂，等船的图 |
| 3 | **删掉 `Resources/UI/Heads`** | 22 张图标全工程零引用，走回收站 |
| 4 | **清临时脚本** | 项目内 `_tmp_*`：0；`.workbuddy` 根目录 `_` 开头：0 |

### ⚠ 我的错：N5 背景提示词写错了
- N5 实际台词：「**搁浅的补给船**歪在礁石里，舱门是被人从里面撬开的，补给箱码得整整齐齐，箱底压着一份泡白的简报」——**文本里没有帐篷、没有营地**。
- 我给的提示词却是「遗迹营地的帆布帐篷 + 作战地图」，那是照**过时策划**写的，**没回头跟代码里的实际台词核对**。
- 已改：`briefing_tent` 作废，`wreck_ship` 接手（正确提示词见 `Docs/待补美术素材与临时项_2026-09-21.md` §三-2）。
- 你那张帐篷图**目前无处可用**，可以删，也可以留着当备用素材。

### ⚠ 我的错：总清单里写「✅ 已有」措辞糊了
那两栏指的是「**提示词**已有」（在另一份配套文档里），**不是图已经有**。
`grave_seven` / `abyss_choice` 的图**一直没出**，所以才一直"还缺"。已在文档里改成「图还没有；提示词已有」。

---

## 二、还等你拍板的

| # | 事项 | 现状 | 我的建议 |
|---|---|---|---|
| ~~1~~ | ~~**`wreck_ship` 出图**~~ | ✅ **图已到位并接入**（2026-09-21 晚）：N5 首句挂了 `.Bg(WreckShip)` | — |
| 2 | 微信「数据统计」「看广告」 | 全是空壳，缺 `.jslib`，`Assets/Plugins` 目录不存在 | 先不做 |
| 3 | 刘海屏 / 挖孔屏适配 | 只挂了 `MainBottomNav`、`QuestHudBar` 两处 | 等真机 |
| 4 | 道具「使用效果」`useEffect` | 只有格式没实现 | 你要求先留接口 |

---

## 三、还缺的美术：~~3 张背景~~ → **0 张（2026-09-21 晚已全部接入）**

| ID | 章节 | 内容 | 状态 |
|---|---|---|---|
| `grave_seven` | N2 第 2 章 Boss 后 | 墓园最里侧，七块无名碑 + 第八块空的 | ✅ 已接入（地名「无名碑」，N2 首句挂 `.Bg()`） |
| `abyss_choice` | N7 三选一前 | 裂隙深处无底深渊边缘，悬空石台 | ✅ 已接入（地名「深渊边缘」，N7 首句挂 `.Bg()`） |
| `wreck_ship` | N5 第 5 章 Boss 后 | 搁浅的补给船（按实际台词写） | ✅ 已接入（地名「搁浅的补给船」，N5 首句挂 `.Bg()`） |

> 现在 10 张剧情背景**全部有图、全部已接**（公会大厅 / 会长办公室 / 4 结局 / N2 / N3 / N4 / N5 / N6 / N7）。
> 立绘 / 图标 / 战斗背景**全齐**，没有隐藏缺口。
> 唯一作废的是 `briefing_tent`（帐篷图对不上台词）—— 图在 `Art/UI/Story/` 与 `Resources/Story/Backgrounds/` 各一份，
> **代码已不引用**，可以删。

---

## 四、已知小毛病（不影响跑）

- 11 条断链已人工确认无害，登记在 `Tools/dangling_allowlist.txt`，不参与 `--strict` 退出码
- `.meta` 形态非法：**0**
- ⚠ 顺手发现（Unity 打开时自动改的，不是我动）：`MercScroll*.png.meta`、`Stamina.png.meta`、`stage_event` 两个 `.meta` 的 guid 被标准化成 hex32 —— 都是按路径加载的资源，**功能无影响**

---

## 五、待清理

- `Tools/prefab-analysis/backup/yongbingsuipian.prefab.bak_20260919`（未忽略且已入库）

---

## 自检

```
python Tools/EnsureMeta.py             → 没有缺少 .meta 的资源 ✓
python Tools/CheckDanglingRefs.py --strict
    未豁免 0 / 已豁免 11 / 形态非法 0，退出码 0 ✅
```

> **未 commit / 未 push**，等你在工程里验证。
